#INCLUDE<STDIO.H>

INT MAIN(VOID)

{

	FILE *FP;

	INT CH;

	FP=FOPEN("MYFILE.TXT","W");

	FPRINTF(FP,"IF EQUAL LOVE THERE CANNOT BE..");

	FPUTC(26,FP);

	FPRINTF(FP,"..LET THE MORE LOVING ONE BE ME\N");

	FCLOSE(FP);

	FP=FOPEN("MYFILE.TXT","R");

	WHILE((CH=FGETC(FP))!=EOF)

		PUTCHAR(CH);

	RETURN 0;

}
